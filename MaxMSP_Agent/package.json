{
  "name": "maxmsp-mcp-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Node bridge helpers and validation scripts for maxmsp-mcp.",
  "main": "max_mcp_node.js",
  "scripts": {
    "check": "node --check max_mcp.js && node --check max_mcp_v8_add_on.js && node --check max_mcp_node.js && node --check bridge_runtime.cjs && node --check protocol.generated.cjs",
    "test": "node --test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.8.1"
  }
}
